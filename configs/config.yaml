defaults:
  - model: clifford
  - machine: hpc
  - _self_

train:
  epochs: 100
  learning_rate: 1e-3
  out_channels: 219

paths:
  train_data: data/processed/train
  val_data: data/processed/test
  model_checkpoint: checkpoints/
